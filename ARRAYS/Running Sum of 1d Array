class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        int n=nums.size();
        int c=0;
        for(int i=1; i<n; i++){
            int m=nums[i-1];
            nums[i]=nums[i]+m;
        }
        return nums;
    }
};
